local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local TS = game:GetService("TeleportService")
local RunService = game:GetService("RunService")
local HttpService = game:GetService("HttpService")
local Lighting = game:GetService("Lighting")
local Workspace = game:GetService("Workspace")
local UserInputService = game:GetService("UserInputService")
local Settings = settings()

if LocalPlayer.PlayerGui:FindFirstChild("ApexKillGUI") then
    LocalPlayer.PlayerGui["ApexKillGUI"]:Destroy()
end

local screenGui = Instance.new("ScreenGui")
screenGui.Name = "ApexKillGUI"
screenGui.ResetOnSpawn = false
screenGui.AutoLocalize = false
screenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")

local frame = Instance.new("Frame")
frame.Size = UDim2.new(0, 180, 0, 100)
frame.Position = UDim2.new(0, 25, 0, 35)
frame.BackgroundColor3 = Color3.fromRGB(45, 15, 85)
frame.BorderSizePixel = 3
frame.BorderColor3 = Color3.fromRGB(140, 60, 230)
frame.Parent = screenGui

local title = Instance.new("TextLabel")
title.Size = UDim2.new(0, 110, 0, 25)
title.Position = UDim2.new(0, 10, 0, 8)
title.BackgroundTransparency = 1
title.AutoLocalize = false
title.Text = "Apex Kill"
title.TextColor3 = Color3.fromRGB(255, 255, 255)
title.TextSize = 18
title.Font = Enum.Font.GothamBold
title.TextXAlignment = Enum.TextXAlignment.Left
title.Parent = frame

local statsLabel = Instance.new("TextLabel")
statsLabel.Size = UDim2.new(0, 60, 0, 25)
statsLabel.Position = UDim2.new(1, -65, 0, 8)
statsLabel.BackgroundTransparency = 1
statsLabel.Text = "PING: -- ms\nFPS: -- FPS"
statsLabel.TextColor3 = Color3.fromRGB(0, 225, 110)
statsLabel.TextSize = 9
statsLabel.Font = Enum.Font.SourceSansBold
statsLabel.TextXAlignment = Enum.TextXAlignment.Right
statsLabel.Parent = frame

local subtitle = Instance.new("TextLabel")
subtitle.Size = UDim2.new(0, 100, 0, 15)
subtitle.Position = UDim2.new(0, 10, 0, 38)
subtitle.BackgroundTransparency = 1
subtitle.AutoLocalize = false
subtitle.Text = "Kills:"
subtitle.TextColor3 = Color3.fromRGB(155, 125, 185)
subtitle.TextSize = 13
subtitle.Font = Enum.Font.Gotham
subtitle.TextXAlignment = Enum.TextXAlignment.Left
subtitle.Parent = frame

local ChronoLabel = Instance.new("TextLabel")
ChronoLabel.Size = UDim2.new(0, 70, 0, 15)
ChronoLabel.Position = UDim2.new(1, -75, 0, 38)
ChronoLabel.BackgroundTransparency = 1
ChronoLabel.Text = "00:00.00"
ChronoLabel.Font = Enum.Font.SourceSans
ChronoLabel.TextSize = 12
ChronoLabel.TextColor3 = Color3.fromRGB(185, 185, 185)
ChronoLabel.TextXAlignment = Enum.TextXAlignment.Right
ChronoLabel.Parent = frame

local killsLabel = Instance.new("TextLabel")
killsLabel.Size = UDim2.new(1, -20, 0, 30)
killsLabel.Position = UDim2.new(0, 10, 0, 58)
killsLabel.BackgroundTransparency = 1
killsLabel.AutoLocalize = false
killsLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
killsLabel.TextSize = 22
killsLabel.Font = Enum.Font.GothamBold
killsLabel.TextXAlignment = Enum.TextXAlignment.Left
killsLabel.Text = "0 (0K)"
killsLabel.Parent = frame

local cStart = os.clock()
RunService.Heartbeat:Connect(function()
    local cElapsed = os.clock() - cStart
    ChronoLabel.Text = string.format("%02d:%02d.%02d", math.floor(cElapsed / 60), math.floor(cElapsed % 60), math.floor((cElapsed * 100) % 100))
end)

task.spawn(function()
    while true do
        local pingVal = math.round(LocalPlayer:GetNetworkPing() * 1000)
        if pingVal <= 0 then pingVal = math.random(15, 45) end
        local fpsVal = math.round(1 / RunService.RenderStepped:Wait())
        if fpsVal > 61 then fpsVal = math.random(58, 61) end
        statsLabel.Text = string.format("PING: %d ms\nFPS: %d FPS", pingVal, fpsVal)
        task.wait(1)
    end
end)

local function formatNumber(v)
    return tostring(v):reverse():gsub("%d%d%d", "%1,"):reverse():gsub("^,", "")
end

task.spawn(function()
    while true do
        task.wait(1)
        local leaderstats = LocalPlayer:FindFirstChild("leaderstats")
        if leaderstats and leaderstats:FindFirstChild("Kills") then
            local k = leaderstats.Kills.Value
            local formattedExact = formatNumber(k)
            local formattedK = string.format("%.1f", k / 1000)
            killsLabel.Text = formattedExact .. " (" .. formattedK .. "K)"
        end
    end
end)

local function getCharacter() 
    return LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait() 
end

local function getRoot() 
    local char = getCharacter()
    return char:WaitForChild("HumanoidRootPart", 5) 
end

local function applySize2()
    local character = LocalPlayer.Character
    local humanoid = character and character:FindFirstChildOfClass("Humanoid")
    if humanoid then
        for _, value in ipairs(humanoid:GetChildren()) do
            if value:IsA("NumberValue") and value.Name:find("Scale") then
                if value.Value ~= 2 then value.Value = 2 end
            end
        end
    end
end

local function teleportToKing()
    local root = getRoot()
    if not root then return end
    local targetCFrame = CFrame.new(-8750, 121, -5859)
    
    root.Velocity = Vector3.zero
    root.AssemblyLinearVelocity = Vector3.zero
    root.CFrame = targetCFrame
    
    local bp = root:FindFirstChild("MaTeUsLock") or Instance.new("BodyPosition")
    bp.Name = "MaTeUsLock"
    bp.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
    bp.Position = targetCFrame.Position
    bp.Parent = root
end

local blockedAnimations = {
    ["rbxassetid://3638729053"] = true,
    ["rbxassetid://3638767427"] = true,
}

local function setupAnimationBlocking()
    local char = LocalPlayer.Character
    if not char or not char:FindFirstChild("Humanoid") then return end
    local humanoid = char:FindFirstChild("Humanoid")

    for _, track in pairs(humanoid:GetPlayingAnimationTracks()) do
        if track.Animation then
            local animId = track.Animation.AnimationId
            local animName = track.Name:lower()
            if blockedAnimations[animId] or animName:match("punch") or animName:match("attack") or animName:match("right") then
                track:Stop()
            end
        end
    end

    if _G.AnimBlockConnection then _G.AnimBlockConnection:Disconnect() end
    _G.AnimBlockConnection = humanoid.AnimationPlayed:Connect(function(track)
        if track.Animation then
            local animId = track.Animation.AnimationId
            local animName = track.Name:lower()
            if blockedAnimations[animId] or animName:match("punch") or animName:match("attack") or animName:match("right") then
                track:Stop()
            end
        end
    end)
end

local function overrideToolActivation()
    local function processTool(tool)
        if tool and (tool.Name == "Punch" or tool.Name:match("Attack") or tool.Name:match("Right")) then
            if not tool:GetAttribute("ActivatedOverride") then
                tool:SetAttribute("ActivatedOverride", true)

                local connection = tool.Activated:Connect(function()
                    task.wait(0.05)
                    local char = LocalPlayer.Character
                    if char and char:FindFirstChild("Humanoid") then
                        for _, track in pairs(char.Humanoid:GetPlayingAnimationTracks()) do
                            if track.Animation then
                                if blockedAnimations[track.Animation.AnimationId] or track.Name:lower():match("punch") then
                                    track:Stop()
                                end
                            end
                        end
                    end
                end)

                if not _G.ToolConnections then _G.ToolConnections = {} end
                _G.ToolConnections[tool] = connection
            end
        end
    end
    
    for _, tool in pairs(LocalPlayer.Backpack:GetChildren()) do processTool(tool) end
    local char = LocalPlayer.Character
    if char then
        for _, tool in pairs(char:GetChildren()) do
            if tool:IsA("Tool") then processTool(tool) end
        end
    end

    if _G.BackpackAddedConnection then _G.BackpackAddedConnection:Disconnect() end
    _G.BackpackAddedConnection = LocalPlayer.Backpack.ChildAdded:Connect(function(child)
        if child:IsA("Tool") then task.wait(0.1) processTool(child) end
    end)
end

task.spawn(function()
    while true do
        local char = LocalPlayer.Character
        if char and char:FindFirstChild("Humanoid") and char.Humanoid.Health > 0 then
            
            for _, track in pairs(char.Humanoid:GetPlayingAnimationTracks()) do
                if track.Animation then
                    local animId = track.Animation.AnimationId
                    local animName = track.Name:lower()
                    if blockedAnimations[animId] or animName:match("punch") or animName:match("attack") or animName:match("right") then
                        track:Stop()
                    end
                end
            end

            local punchInBackpack = LocalPlayer.Backpack:FindFirstChild("Punch")
            local punchInCharacter = char:FindFirstChild("Punch")
            
            if punchInBackpack then
                punchInBackpack.Parent = char
                if punchInBackpack:FindFirstChild("attackTime") then
                    punchInBackpack.attackTime.Value = 0
                end
            end
            
            if punchInCharacter then
                if punchInCharacter:FindFirstChild("attackTime") then
                    punchInCharacter.attackTime.Value = 0
                end
                punchInCharacter:Activate()
            end
            
            local tool = char:FindFirstChildOfClass("Tool")
            if tool and (tool.Name:lower():find("punch") or tool.Name:lower():find("attack")) then
                pcall(function()
                    for i = 1, 3 do 
                        tool:Activate()
                    end
                end)
            end

            local rightHand = char:FindFirstChild("RightHand")
            local leftHand = char:FindFirstChild("LeftHand")

            if rightHand and leftHand then
                for _, target in ipairs(Players:GetPlayers()) do
                    if target ~= LocalPlayer and target.Character then
                        local targetRoot = target.Character:FindFirstChild("HumanoidRootPart")
                        local targetHumanoid = target.Character:FindFirstChild("Humanoid")
                        
                        if targetRoot and targetHumanoid and targetHumanoid.Health > 0 then
                            pcall(function()
                                for i = 1, 3 do 
                                    firetouchinterest(rightHand, targetRoot, 1)
                                    firetouchinterest(leftHand, targetRoot, 1)
                                    firetouchinterest(rightHand, targetRoot, 0)
                                    firetouchinterest(leftHand, targetRoot, 0)
                                end
                            end)
                        end
                    end
                end
            end
        end
        task.wait(0.02)
    end
end)

task.spawn(function()
    while true do
        applySize2()
        task.wait(1)
    end
end)

local function checkAndHideGui(gui)
    if gui:IsA("TextLabel") or gui:IsA("ImageLabel") or gui:IsA("Frame") then
        if gui:IsDescendantOf(frame) or gui.Name == "ApexKillGUI" then return end
        local nameLower = gui.Name:lower()
        local textLower = gui:IsA("TextLabel") and gui.Text:lower() or ""
        
        if nameLower:find("push") or nameLower:find("gain") or nameLower:find("popup") or nameLower:find("clone") or 
           nameLower:find("notification") or textLower:find("durabilidade") or textLower:find("durability") or 
           textLower:find("+") or textLower:find("kill") or textLower:find("ganhou") then
            
            gui.Visible = false
            if gui:IsA("TextLabel") then gui.TextTransparency = 1 end
            if gui:IsA("ImageLabel") then gui.ImageTransparency = 1 end
        end
    end
end

local playerGui = LocalPlayer:WaitForChild("PlayerGui")
for _, g in pairs(playerGui:GetDescendants()) do checkAndHideGui(g) end
playerGui.DescendantAdded:Connect(checkAndHideGui)

local function cleanPart(v)
    if v:IsA("BasePart") or v:IsA("MeshPart") or v:IsA("UnionOperation") then
        v.Material = Enum.Material.Plastic
        v.Reflectance = 0
        v.CastShadow = false
    elseif v:IsA("ParticleEmitter") or v:IsA("Trail") or v:IsA("Beam") or v:IsA("Fire") or v:IsA("Smoke") or v:IsA("Sparkles") then
        v.Enabled = false
    elseif v:IsA("Decal") or v:IsA("Texture") then
        v.Transparency = 1
    end
end

local function optimizePerformance()
    Settings.Rendering.QualityLevel = Enum.QualityLevel.Level01
    Lighting.GlobalShadows = false
    Lighting.Brightness = 0.5
    Lighting.ClockTime = 5.5
    Lighting.EnvironmentDiffuseScale = 0
    Lighting.EnvironmentSpecularScale = 0

    if Lighting:FindFirstChild("Sky") then Lighting.Sky:Destroy() end
    for _, v in pairs(Workspace:GetDescendants()) do cleanPart(v) end
    Workspace.DescendantAdded:Connect(cleanPart)
end

task.spawn(function()
    while true do
        Lighting.GlobalShadows = false
        Settings.Rendering.QualityLevel = Enum.QualityLevel.Level01
        task.wait(5)
    end
end)

local toggled = true
UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if gameProcessed then return end
    if input.KeyCode == Enum.KeyCode.F then
        toggled = not toggled
        if playerGui:FindFirstChild("Chat") then
            playerGui.Chat.Frame.Visible = toggled
        end
    end
end)

local function initCharacter()
    task.wait(0.2)
    teleportToKing()
    setupAnimationBlocking()
    overrideToolActivation()
end

task.spawn(initCharacter)
LocalPlayer.CharacterAdded:Connect(function()
    initCharacter()
end)

task.spawn(optimizePerformance)
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local VirtualUser = game:GetService("VirtualUser")
local RunService = game:GetService("RunService")

game:GetService("Players").LocalPlayer.Idled:Connect(function()
    VirtualUser:CaptureController()
    VirtualUser:ClickButton2(Vector2.new())
end)

local function hideAllFrames(bool)
    for _, obj in pairs(ReplicatedStorage:GetChildren()) do
        if obj.Name:match("Frame$") then
            obj.Visible = not bool
        end
    end
end
hideAllFrames(true)

_G.AutoSpinWheel = true
spawn(function()
    while _G.AutoSpinWheel do
        local remote = ReplicatedStorage:FindFirstChild("rEvents") and ReplicatedStorage.rEvents:FindFirstChild("openFortuneWheelRemote")
        if remote then
            remote:InvokeServer("openFortuneWheel", ReplicatedStorage.fortuneWheelChances["Fortune Wheel"])
        end
        task.wait(1) 
    end
end)

--Camarero 
local function ExecuteRealServerHop()
    local nextCursor = ""
    local serverFound = false
    
    while not serverFound do
        local url = "https://games.roblox.com/v1/games/" .. game.PlaceId .. "/servers/Public?sortOrder=Desc&limit=100"
        if nextCursor ~= "" then url = url .. "&cursor=" .. nextCursor end
        
        local success, result = pcall(function() return game:HttpGet(url) end)
        if success and result then
            local decoded = HttpService:JSONDecode(result)
            if decoded and decoded.data then
                local list = decoded.data
                for i = #list, 2, -1 do
                    local j = math.random(i)
                    list[i], list[j] = list[j], list[i]
                end
                
                for _, server in ipairs(list) do
                    if server.id ~= game.JobId and server.playing and server.maxPlayers then
                        if server.playing < server.maxPlayers and server.playing >= 5 then
                            serverFound = true
                            pcall(function() TS:TeleportToPlaceInstance(game.PlaceId, server.id, LocalPlayer) end)
                            task.wait(2)
                            if not serverFound then break end
                        end
                    end
                end
                if decoded.nextPageCursor then nextCursor = decoded.nextPageCursor else break end
            else break end
        else break end
        task.wait(1)
    end
    if not serverFound then pcall(function() TS:Teleport(game.PlaceId, LocalPlayer) end) end
end

task.spawn(function()
    while true do
        task.wait(45)
        ExecuteRealServerHop()
    end
end)
